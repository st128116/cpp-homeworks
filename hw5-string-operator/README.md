# HW5 — String operator[]

Реализация оператора [] для класса String.

Поддерживается выражение:
str[i][j]

Возвращается подстрока с позиции i (включительно)
по позицию j (не включая).

## Структура
- `src/main.cpp` — реализация
- `Makefile` — сборка

## Сборка и запуск

```bash
make
./hw5_string
